（1）使用串行化隔离级别，进行范围查询、无索引查询或非唯一索引查询时会对整张表加锁，强制事务串行执行。
（2）可重复读隔离级别下，通过临键锁来缓解幻读，临键锁由行锁和间隙锁组成，如果等值查询的记录存在则加行锁，如果等值查询不存在则需要确定在哪个范围区间中，可以是第一条记录前面所有的区间、两条记录之间的区间，以及最后一条记录后面的区间，然后根据确定的区间加间隙锁。范围查询也是同理，会对范围查询条件覆盖的记录之间的区间都上锁。