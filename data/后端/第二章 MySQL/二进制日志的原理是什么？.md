**二进制日志是什么：**完成一次更新操作后，服务层会生成一条binlog记录，事务提交时会将事务产生的所有binlog写入binlog日志。主要用于备份恢复和主从复制。

**原理：**binlog文件记录了所有数据库表结构变更和表数据修改的日志，不会记录查询类操作，binlog有三种格式类型：
- STATEMENT：每一条修改的SQL语句
- ROW：记录修改后的最终数据
- MIXED：包含了STATEMENT和ROW两种模式，根据不同情况自动选择用STATEMENT或ROW