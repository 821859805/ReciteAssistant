上电初始化，给硬件供电，并取BIOS入口代码的地址

运行BIOS代码，检查硬件资源是否正常、初始化硬件、按照BIOS设置的顺序找到可引导的磁盘

寻找主引导记录MBR，BIOS找到硬盘后，会读取第一个扇区，也就是MBR主引导记录，执行里面的代码，找到活动分区，加载分区里的 BootLoader

内核初始化，有了BootLoader之后，将内核加载到内存里面

内核运行第一个用户进程，init进程，负责初始化网络、SSH等服务，以及挂载文件系统

进入用户界面


