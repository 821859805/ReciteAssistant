MMU是Memory Management Unit 内存管理单元，是集成在CPU内部的硬件模块，核心作用如下：

第一，将虚拟地址转化为物理地址

第二，对某段内存进行内存权限控制

第三，实现分页和虚拟内存

MMU由以下模块组成：

TLB：快表：加速地址翻译，避免每次都查页表

页表遍历硬件：快表不命中时，遍历多级页表

地址加法器：将页号+业内偏移量拼接成最终物理地址

权限检查模块：检查是否有缺页异常、段异常

控制寄存器：存放页表基地址