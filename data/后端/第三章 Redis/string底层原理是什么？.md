Redis的字符串是用SDS数据结构来存储的，有四个字段：字符串长度、分配的空间长度、sds类型、字符数组
- 字符串长度：获取字符串长度时间复杂度O(1)
- 分配的空间长度：修改字符串的时候，如果空间不够，则可以自动扩容，不会发生缓冲区溢出
- sds类型，一共有五种sds类型：sdshdr5、sdshdr8、sdshdr16、sdshdr32、sdshdr64
- 字符数组，不仅可以保存字符串，也可以保存二进制数据