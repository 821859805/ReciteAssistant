**触发条件：**
（1）负载因子大于等于1，且Redis未处于RDB持久化和AOF重写阶段
（2）负载因子大于等于5，无论是否处于持久化阶段，强制扩容

**原理：**
（1）给哈希表2分配哈希表1两倍的空间
（2）每次对哈希表元素新增、删除、查找或更新操作时，Redis还会将哈希表1的对应桶中的键值对迁移到哈希表2上，这叫做渐进式哈希，避免STW过长。
（3）哈希表1的空间被释放，将哈希表2设置为哈希表1