一共有五个状态：运行、可中断睡眠、不可中断睡眠、暂停态、僵尸态
运行态：正在运行，或者就绪队列等待CPU
可中断睡眠：等待信号、IO或者锁，可被信号唤醒
不可中断睡眠：等待磁盘IO或网络IO，不能被信号打断，kill -9也杀不死
暂停状态：被Ctrl+Z暂停
僵尸态：子进程退出，父进程没有调用wait()或waitpid()回收，导致PCB还留在系统进程表里面

查看进程状态：
ps aux #查看当前系统进程，a表示所有用户的进程、u以用户友好的格式显示，x是没有控制终端的进程
ps -ef