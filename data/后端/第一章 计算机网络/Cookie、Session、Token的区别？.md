Cookie：存储在客户端本地，一般用于存储用户的身份信息，一段时间内会过期。

Session：服务器创建Session后，会生成唯一的SessionID，浏览器每次请求都会将SessionID放到Cookie里，并发送给服务器，服务器将内存中存储的SessionId与请求方的进行对比，然后进行响应。浏览器关闭后，Session就被清除了

Token：是一种无状态的令牌，包含了用户的信息，每个服务器对相同的令牌的解析结果都是一样的。