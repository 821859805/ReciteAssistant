第一，选举流程，一开始整个系统没有Leader，都是Follower，每个Follower都设置了随机超时时间，当其中一个Follower发生超时后，向其他节点发送投票请求，并为自己投一票，最终选择获得最多票数的节点升级为Leader，如果有并列第一，则重置两者的计时器，进入第二轮选举

第二，Leader复制日志，由于客户端所有的写请求只能由Leader处理，日志复制是保证一致性的核心，Leader将客户端命令追加到日志末尾，并向所有Follower发送追加日志RPC请求，携带新日志项；Follower会处理这个请求，追加日志；如果多数节点都复制成功，Leader才会提交