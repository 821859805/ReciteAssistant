应用层：通过DNS协议获取域名对应的ip地址，构造HTTP/HTTPS请求报文，把数据交给传输层

传输层：将HTTP封装为TCP报文段，加上源端口和目的端口，建立TCP连接，并把报文段交给网络层

网络层：将TCP报文段封装为IP数据报，加上源IP和目的IP，选择路由确认下一跳，交给数据链路层

数据链路层：将IP数据报封装成帧，加上源MAC和目的MAC地址（通过ARP协议获取），转化为比特流，交给物理层

物理层：物理层把帧转化为电信号/光信号/无线信号进行传输，到达目标服务器的网卡

目标服务器走刚才相反的操作，依次解封装。然后将请求的内容再原路返回