（1）本质区别：HTTP3基于QUIC协议，是建立在UDP上的可靠传输协议，HTTP2是基于TCP协议
（2）功能上：
第一，队头阻塞问题上，TCP有队头阻塞问题，单个流丢包会阻塞所有流，QUIC是独立的流，一个流丢包只影响自身
第二，建立连接问题上，TCP建立连接延迟高，需要两到三个RTT，QUIC协议握手和加密过程合并，首次连接1RTT，复用会话时0RTT
第三，发生移动网络和WIFI之间的迁移时，由于TCP依赖（IP地址和端口号），网络迁移时，必然发生改变，TCP会立即中断需要重建，QUIC通过连接ID标识连接，无缝进行网络迁移