（1）头部压缩：如果同时发送了多个请求，且多个请求的头是一样的，那么HTTP2会自动帮忙消除重复的部分。具体实现：客户端和服务器会维护一张头信息表，所有头部都会存到这张表中并生成相应的索引号，发送请求只需要发送索引号就行了。

（2）二进制格式：HTTP1.1只支持纯文本格式，HTTP2的请求头和请求体都是二进制格式，而且都统称为帧，对于计算机传输来说性能很高。

（3）并发传输：HTTP2引入了Stream的概念，多个Stream复用一条TCP连接，解决了HTTP1.1的队头阻塞的问题。

（4）服务器主动推送：HTTP2支持服务器主动向客户端发送消息。