第一步，客户端向服务器发送ClientHello请求，发送**加密套件**、**TLS版本**和**第一个随机数**

第二步，服务端返回SeverHello的响应，向客户端发送：确认TLS版本、确认加密套件、第二个随机数，证书和公钥，由于服务器发送了多个响应，最后还需要发送ServerHelloDone表示发送完毕

第四步，

（3）第三步，校验服务器证书合法性、从证书中取出公钥，再生成第三个随机数，用公钥加密这个随机数来生成预主密钥，通知服务器以后的信息都要加密

（4）第四步，服务器用私钥解密预主密钥

（5）双方用随机数+预主密钥计算出会话密钥