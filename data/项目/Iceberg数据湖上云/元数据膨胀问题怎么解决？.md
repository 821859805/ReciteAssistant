第一，元数据合并，我们的元数据文件一共有三种，第一个记录了数据文件的路径，第二个是快照，第三个是元数据文件，频繁写入的场景下这些文件往往都只记录了一个条目，我们可以将所有的文件都合并为一个，写入新的元数据文件，并在MySQL中记录变更。

第二，快照过期，元数据合并只是创建了新的元数据，并没有删除旧的元数据文件，现在我们可以选择保留几个快照，把这些快照之前的旧快照都删除掉

第三，孤儿文件清理，这是我们的兜底方案，清理所有没有被Iceberg引用的文件，也就是孤儿文件。