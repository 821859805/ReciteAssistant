（1）最核心的问题是准确性差：LLM很容易出现幻觉，引用不存在的表或字段；对于复杂业务逻辑，直接生成的SQL可能不符合预期，比如出现逻辑错误和语法错误
（2）可能出现危险操作：比如删除表、删除数据没加where条件。查询全表的数据，我们有千亿级别的数据，查询全表会让系统直接崩溃
（3）方便适配不同数据源：我们使用Doris查询数据，和MySQL的语法有差别，比如Doris完整表名是catalog.database.tableName三级结构，而MySQL的完整表名是database.tableName两级结构，使用语义SQL中间态能够方便我们对SQL语句进行调整