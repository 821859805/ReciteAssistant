**是什么：**我们的文件上传、工具调用和工作流执行这些操作如果都放到后端运行，不但会给后端带来巨大的压力，而且一旦出现错误的指令或者恶意指令，很容易将CPU、内存甚至磁盘打满，导致后端服务直接崩溃，无法服务，为了解决这个问题，我们需要一个能够提供Agent稳定、安全、隔离的工作运行时环境，这就是沙箱

**怎么做：**
沙箱提供了以下环境：
（1）文件系统：用户上传文件都是上传到用户工作空间的文件系统里面
（2）Python虚拟环境：工具调用有可能需要执行Python代码，每个用户的工作空间里都需要配备一个Python虚拟环境
（3）对外提供文件操作和命令执行的API