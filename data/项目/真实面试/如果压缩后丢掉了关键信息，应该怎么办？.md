第一，压缩前。压缩前让llm主动把决策结论、用户偏好、关键数据写入文件或向量库

第二，压缩中。对于用户的消息，永不修改，保留原文；对于最近N轮对话，不压缩；对于旧的工具调用结果，将结果前1500字符和后1500字符保留，中间用...代替；对于旧的对话，分成多个chunk，逐个chunk用llm生成摘要，多个chunk摘要进行滚动合并

第三，压缩后。需要将摘要持久化到磁盘中，并且后续可以通过memory_search将丢失的记忆找回，可以在提示词里面写着：回答涉及之前的工作、决策、偏好时，先搜索记忆