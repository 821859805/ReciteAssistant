第一层，针对Read工具调用结果进行会话裁剪。当上下文占用达到窗口的30%时触发，对超过4000字符的旧工具结果，只保留头部的1500字符和尾部的1500字符，中间用...代替。比如读取了Skill.md，会仅仅保留3000字符；当上下文达到50%的时候，直接把所有的旧工具结果替换为一句话“Old tool result content cleared”；

第二层，针对MCP工具的调用结果进行会话裁剪。将查询结果生成文件并且放到用户的沙箱空间中，只返回几条记录给LLM进行总结，如果需要，通过grep命令查询文件具体内容

第三层，对整个对话历史压缩。如果上下文接近或超过模型窗口的时候，让LLM把之前的对话历史分为多个分块chunk，对逐个chunk调用llm生成摘要，最终替换为一条压缩摘要和最近完整的消息