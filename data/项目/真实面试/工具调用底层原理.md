第一、在向大模型发送消息的时候会构建消息请求体，这个请求体字符串主要包含三个字段：模型、消息和工具。这个工具字段是核心，告诉了大模型有哪些工具，工具名、工具参数、工具描述分别是什么

第二、大模型收到消息的时候，会进行推理，并返回一个响应体，这个响应体里面有一个关键的字段choices，代表了模型选择了什么工具，工具名和参数分别是什么

第三、程序收到大模型返回的消息后，解析响应体，提取其中的工具名和工具参数，再调用对应的API真正实现工具调用

总结，并不是真的让大模型调用工具，而是让大模型返回工具名和参数，然后由我们程序来调用工具，黑盒上看就像AI调用工具