**正常模式：**默认模式，请求锁的协程和等待队列头部的协程竞争，信赖的协程有几次自旋的机会，如果在此期间锁被释放，可以直接抢到锁，这种方式吞吐量高，但是会让队列中的协程等待很久，不公平

**饥饿模式：**一个协程在等待队列中等待超过1毫秒后，Mutex切换为饥饿模式，讲究公平，新来的协程不会自旋，必须排到队列尾部。

**模式切换：**等待队列为空，或一个协程拿到锁时发现它的等待时间小于1毫秒，切换回正常模式