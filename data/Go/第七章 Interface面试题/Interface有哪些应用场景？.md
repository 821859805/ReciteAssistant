**why：**
- **解耦合：**接口可以隔离不同模块的直接依赖，比如订单模块只依赖支付接口，不依赖具体支付方式

- **多态：**定义一个`Shape`接口并包含`Area()`方法，不同的图形需要具体实现这个接口，就能用统一的方式处理不同的图形，增加代码的可扩展性

- **统一API**：像`io.Reader`、`io.Writer`让文件、网络连接、字符串等都能用统一的方式操作

- **类型断言、反射：**JSON解析，先用`Interface{}`接收任意类型，再通过类型断言或反射处理具体逻辑
