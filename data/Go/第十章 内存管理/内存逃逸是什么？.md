**what：**内存逃逸是编译器在程序编译时期根据逃逸分析策略，将原本应该分配到栈上的对象分配到堆上的一个过程。因为堆对象需要垃圾回收机制来释放内存，栈对象会跟随函数结束被编译器回收，所以大量的内存逃逸会给gc带来压力。

**how：**
- 函数返回内部变量的地址，变量会逃逸到堆上
- 传递给interface{}参数的具体类型会逃逸，因为需要运行时类型信息
- 被闭包捕获的变量会逃逸到堆上
- 当容量超出编译器确定范围时会逃逸
- 超过栈大小限制的对象直接分配到堆上