**是什么**：完全无序，每次都会随机选择一个桶，每个桶中，也是随机遍历，因此是无序的。

**为什么：**map扩容后，会发生key的迁移，key可能不会在原来的位置，如果遍历时按顺序遍历桶和key，遍历map的结果就可能不会按照原来的顺序。go官方为了避免程序员写出脆弱的代码，故意设置是无序的。

**怎么做：**可以将key取出来放进切片中，用sort包对切片排序，然后根据有序切片遍历map