**触发条件：**向map插入新的key时，如果符合以下两个条件就会扩容：
（1）装填因子超过阈值6.5时，触发双倍扩容
（2）当buckets的总数小于2的15次方且overflow的数量大于buckets
（3）当buckets的总数大于等于2的15次方且overflow的数量大于2的15次方

**扩容机制：**扩容不是一次性完成，而是渐进式迁移，首先会初始化新的桶，正常扩容时新桶长度为旧桶长度2倍，等量扩容时，数组长度和原来一样。然后每次增删改查都只是迁移一两个桶，这样能够均摊整个迁移操作，避免长时间STW