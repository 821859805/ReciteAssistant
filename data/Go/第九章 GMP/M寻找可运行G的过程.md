**how：**
（1）M首先从当前P的LRQ里runqget一个G，这个过程是CAS轻量级自旋锁
（2）如果本地队列没有可运行G，再次检查全局队列（GRQ）去全局队列里globrunqget找，需要加全局锁
（3）如果全局队列没有，就检查网络轮询器netpoll，就去netpoll里面看看有没有因为网络IO就绪的G。
（4）如果网络轮询器也没有，则会随机找一个别的P偷一半G过来，需要加被偷P的私有自旋锁