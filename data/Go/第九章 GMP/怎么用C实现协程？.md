定义核心结构体：第一个是状态结构体，包括就绪、运行、挂起、结束四个字段；第二个是协程结构体，包含协程上下文、协程栈、栈大小、协程状态、协程函数和参数；第三，调度器结构体，包含主线程上下文和当前运行的协程

定义协程创建逻辑：包括分配协程栈、初始化上下文，设置协程执行函数和参数

定义协程调度与切换逻辑，包含启动、挂起和销毁：
- 启动协程，将当前主线程切换到协程
- 协程挂起，从协程切换为主线程
- 销毁协程，释放协程内存资源