**what**：g0是调度协程，每个m都有自己的g0，使用系统线程的原始栈空间，而不是普通协程那样使用可增长的分段栈， G0的栈大小通常是8kb，比普通协程的2kb初始栈要大

**why：**：因调度器代码不能在普通的协程栈上执行，那样会有占空间冲突和递归调用的问题， G0提供了一个独立的执行环境，确保调度器能够安全稳定的工作。

**how：**：负责执行调度逻辑，包括协程的创建、销毁、调度决策等。当m需要进行调度时，会从当前运行的用户协程切换到g0，并执行schedule函数。 G0还负责垃圾回收，栈扫描、信号处理等运行时操作
