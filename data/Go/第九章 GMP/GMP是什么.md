**what**：G是协程，M是操作系统线程，P是逻辑处理器
**why**: GMP模型让少量线程能海量调度协程，是go高并发基础。
**how**，调度逻辑：操作系统线程必须绑定逻辑处理器才能执行协程，每个逻辑处理器维护一个自己的本地协程队列（长度256），操作系统线程从协程队列取协程执行，本地队列为空时，操作系统线程会按优先级从全局队列，网络轮询器，其他逻辑处理器队列中窃取协程，这就是work-stealing机制