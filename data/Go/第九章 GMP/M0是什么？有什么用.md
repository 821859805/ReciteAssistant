**what**：M0是go启动时创建的第1个M， M0对应程序启动时的主系统线程，在go的整个生命周期中都存在。与其他通过runtime.new（）动态创建的m不同， M0是在程序初始化阶段静态分配的，有专门的全局变量存储
**why**：主要负责执行go程序的启动流程，包括调度器初始化，内存管理器初始化，垃圾回收器设置等，它会创建并运行第1个用户goroutine来执行main.main函数。在程序运行期间， M0也参与正常的goroutine调度，和其他m没有本质区别。 M0在程序退出时还会负责处理清理工作，比如等待其他goroutine结束，执行defer函数等。