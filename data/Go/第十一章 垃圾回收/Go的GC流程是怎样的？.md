一、准备阶段：首先STW暂停用所有户协程，开启写屏障，扫描根对象初始快照

二、并发标记：从GC Root开始，遍历所有可达堆对象，利用三色标记法算出黑色和白色对象，写屏障拦截用户修改引用的操作，将被引用的对象标记为灰色

三、标记终止阶段：开启STW暂停所有用户协程，处理少量灰色对象，确保没有遗漏，关闭写屏障

四、内存清扫阶段：清理所有的白色对象

五、内存归还阶段：如果堆内存使用量高于实际需求，会将部分内存归还给操作系统