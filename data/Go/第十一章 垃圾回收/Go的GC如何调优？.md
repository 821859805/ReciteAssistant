**how：**
- 观测，通过gctrace、pprof、trace确认哪里出了问题，是GC频繁、STW过长，还是内存持续上涨

- 环境变量GOGC（默认100）：含义为当堆内存超过上一次GC后堆大小的百分比时触发GC，默认是超过一倍触发GC。增大参数迎合内存充足、CPU紧张的场景；减小参数适合内存紧张，CPU充足的场景

- 使用对象池sync.Pool复用高频创建的对象，减少内存逃逸、内存泄漏