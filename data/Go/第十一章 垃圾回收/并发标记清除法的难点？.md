**what：**核心难点在于如何保证用户程序并发修改对象引用时，垃圾回收器正确识别存活对象

**why：**
- **对象消失：**标记过程中，如果用户程序删除了黑色到白色对象的引用+灰色到白色的引用，假如此时发生GC，这个白色对象会被错误回收，但是用户后面可能还会使用这个对象，此时会发现该对象不存在。

**how：**
- **强三色不变式**：黑色对象不能引用白色对象。因为黑色已经是确认存活的，如果黑色引用白色则会导致白色漏标。
- **混合写屏障：**新建引用时将目标对象标记为灰色，删除引用时也将被删对象标记为灰色