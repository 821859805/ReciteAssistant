JMM是Java的内存模型，是解决多线程并发问题的一套规则，保证线程访问变量的可见性、原子性、有序性。

- 可见性：一个线程修改了变量还没来得及写回主存，另一个线程读取的是脏数据，使用volatile关键字能够保证立即刷新到主存，保证可见性

- 原子性：使用多线程来卖票，一个线程if判断有票，此时切换为另一个线程，另一个线程也判断有票，因此两个线程都能进入卖票的逻辑，如果此时只有1张余票，那么就会导致超卖问题，因此需要通过锁机制来保证原子性

- 有序性：编译器或者CPU为了提速，可能会调整指令顺序，多线程下，可能会导致逻辑错误，可以通过volatile或加锁来通过内存屏障阻止重排序