**是什么：**Java内置的监视器锁，在同步代码块前后加上monitorenter和monitorexit指令，底层依赖操作系统互斥锁来实现共享变量或代码块。

**原理：**
（1）多个线程进入同步代码块时，先进入entryList
（2）当一个线程获取到监视器锁的时候，计数器+1，这个计数器保证了一个线程可以重复进入这个代码块，也就是说，监视器锁具有可重入的特点
（3）如果线程调用wait方法，则释放该锁，计数器-1，并进入waitSet等待调用notify或notifyAll方法被唤醒，重新进入entryList获取锁